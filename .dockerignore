# В контекст сборки Docker попадает только нужное.
# Скриншоты превью (~5 МБ), документация и служебные каталоги не нужны в образе.
preview-*.png
README.md
*.zip
.git
.codewhale
# локальные данные админ-панели (в контейнере — отдельный volume)
data
